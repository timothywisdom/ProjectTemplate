import React from 'react';

const {{ pascalCase name }}: React.FC = () => {
    return (
    	<div id="{{{pascalCase name}}}">{{ pascalCase name }} Component</div>
    );
};

export default {{ pascalCase name }};
